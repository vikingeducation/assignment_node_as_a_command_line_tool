var cp = require('child_process');


// I did this as both `exec` and `spawn` in two separate
// files instread of this single one. See:

// child_processes_with_exec.js
// child_processes_with_spawn.js
